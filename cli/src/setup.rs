use crate::output;

// TODO: Clone Solana from `buffalojoec/solana` on the proper branch and set up
// the CLI
pub fn setup() {
    output::starting_setup();
    output::starting_local_validator();
}
